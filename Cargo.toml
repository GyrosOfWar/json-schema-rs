[package]
authors = ["Martin Tomasi <martin.tomasi@gmail.com>"]
name = "json_schema"
version = "0.1.0"

[dependencies]
error-chain = "0.10"
json = "0.11"
regex = "0.2"
lazy_static = "0.2"
chrono = "0.3"

[dependencies.clippy]
optional = true
version = "*"

[features]
default = []
